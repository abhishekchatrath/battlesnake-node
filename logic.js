//there is nothing here right now

let game_id = 0;
let width = 0;
let height = 0;

startFunc = () => {
    
};

moveFunc = () => {

};

setupGame = (game_id, width, height) => {
    game_id = game_id;
    width = width;
    height = height;
};

export {startFunc, moveFunc, setupGame};